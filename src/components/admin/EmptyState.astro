---
interface Props { message?: string; }
const { message = 'Nothing here yet.' } = Astro.props;
---
<div class="rounded border border-dashed border-[#DDDDD8] bg-white py-16 text-center text-sm text-[#888]">
  {message}
  <slot />
</div>
